<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.raptors.russian_museum">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.RussianMuseum">
        <activity
            android:name=".guess_genre.activity.GuessGenreActivity"
            android:exported="false" />
        <activity
            android:name=".puzzles.activities.PuzzlesActivity"
            android:exported="false" />
        <activity
            android:name=".puzzles.activities.PuzzleActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".find_object.activities.FindObjectActivity"
            android:exported="false" />
        <activity
            android:name=".guess_genre.activity.GuessGenreChooseActivity"
            android:exported="false" />
    </application>

</manifest>